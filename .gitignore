target/
dbt_packages/
logs/
dbt_modules/
profiles.yml
.DS_Store
.vscode/
dbt_internal_packages/

# generated, never committed -- reproduce with `python -m sim.generate`
data/
!data/.gitkeep

__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
# Figures are regenerated by `python -m ml.run`, so they are build artefacts --
# except the two the README embeds. Those are committed, because a repo whose
# landing page shows two broken images is worse than one with no images at all.
reports/figures/*.png
!reports/figures/coefficient_recovery.png
!reports/figures/temperature_response.png
!reports/figures/.gitkeep
.ruff_cache/
.user.yml
